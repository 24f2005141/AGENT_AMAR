# Secrets & credentials — never commit
.env
.env.*
!.env.example
.tokens/
*.token.json
secrets/
client_secret*.json

# Local databases (Phase 9)
*.db
*.sqlite
*.sqlite3

# Local ML pre-classifier — build artifacts + your private training data.
# The bundled starter set (email_training_data.sample.jsonl) IS tracked.
data/models/
data/training/email_training_data.jsonl

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
