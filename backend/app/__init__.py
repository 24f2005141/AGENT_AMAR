"""AGENT AMAR backend package.

Phase 2, slice 1: a small, deterministic pipeline that turns a raw Gmail API
message into the normalized email object defined in the Obsidian vault
(`04-Schemas/Email Schema.md`).

No Triage / Action / Deadline / Priority agents, no database, no notifications,
and no LLM calls live here yet.
"""

__version__ = "0.1.0"
